from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Given data in a country, predict for one day the number of dead / covid cases',
    author='Jordan BEZIAUD',
    license='MIT',
)
